import 'package:flutter/material.dart';

class Like_screen extends StatelessWidget {
  const Like_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Like"),
      ),
    );
  }
}